# Assistive Smart Glove Backend

Minimal Rust API scaffold for future scan ingestion, auth, and device telemetry.

Run:

```bash
cargo run
```

Health check:

```text
GET http://127.0.0.1:8080/health
```
